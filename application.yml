eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  rabbitmq:
    host: localhost    
    port: 5672
    username: guest
    password: guest   
management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  cloud:
    config:
      uri: http://localhost:8999

refreshUrl : http://121.128.108.76:10888/monitor
